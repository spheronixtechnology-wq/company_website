I will undo all the changes made in the previous step and restore the files to their original state.

**1. Revert `Partners.tsx`**
*   I will restore the original "Top Companies" section that uses the dynamic list of companies (Swiggy, Flipkart, etc.) with the infinite scrolling marquee and Clearbit logos.

**2. Revert `OfficeEnvironment.tsx`**
*   I will restore the original "Real Office Environment" section that uses the high-quality Unsplash images for the office features (Workspace, Workstations, Culture, Onboarding).

**3. Revert `Certifications.tsx`**
*   I will restore the original "Recognized by International Bodies" section that uses the standard icons (ISO, Startup India, etc.) instead of image placeholders.

**4. Clean up Directories**
*   I will remove the `assets/office` and `assets/certificates` directories that were created.

This will bring the application back to the exact state it was in before the image updates.
