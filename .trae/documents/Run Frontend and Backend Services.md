To run the application, we need to set up and start both the frontend (React/Vite) and the backend (Node/Express) services.

Here is the plan:

1.  **Install Frontend Dependencies**:
    *   Run `npm install` in the root directory (`c:\Users\kiran\OneDrive\Desktop\compnay website\spheronix-technology-website backup\spheronix-technology-website`).
2.  **Install Backend Dependencies**:
    *   Run `npm install` in the `server` directory (`c:\Users\kiran\OneDrive\Desktop\compnay website\spheronix-technology-website backup\spheronix-technology-website\server`).
3.  **Start the Backend Server**:
    *   Run `node index.js` in the `server` directory. This will start the API server on port 5000.
4.  **Start the Frontend Development Server**:
    *   Run `npm run dev` in the root directory. This will start the Vite server on port 3000.

I will execute these steps to get the application running for you. I will use separate terminals or background processes to ensure both run simultaneously.
